# Public Supabase settings (safe to ship in the browser bundle — never put the service_role key here)
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<publishable-or-anon-key>
